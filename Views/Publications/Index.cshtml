@model IEnumerable<Book_MVC.Models.Publications>

@{
    ViewData["Title"] = "Index";
}
<body>
    <div class="text-center">
        <header>
            <!--z-index code to display image on the Author page behind the text-->
            <style>
                img {
                    position: absolute;
                    left: 0px;
                    top: 0px;
                    z-index: -1;
                    display: block;
                    width: 100%;
                    height: 100%;
                    object-fit: cover;
                }
            </style>
        </header>

        <img src="~/Images/book 3.jpg" />
        <h1>Index</h1>

        <p>
            <a asp-action="Create">Create New</a>
        </p>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Books_Copies)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Publisher_detail)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Books_detail)
                    </th>
                    <th></th>
                </tr>
            </thead>
         
        </body>
            <body>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Books_Copies)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Publisher_detail.Publisher_Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Books_detail.Tittle)
                        </td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                            <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                            <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                        </td>
                    </tr>
                }
            </body>
        </table>
